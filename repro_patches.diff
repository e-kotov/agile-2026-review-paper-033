=== Reproducibility Patch Set for Paper 033 ===
Generated on: Tue Apr 28 17:58:48 CEST 2026
----------------------------------------------------------------------

PATCH FOR: requirements.txt
======================================================================
--- ORIGINAL/requirements.txt
+++ MODIFIED/requirements.txt
@@ -15,4 +15,4 @@
 wordcloud==1.9.6
 ipykernel==7.1.0
 jupyterlab==4.5.0
-geoalchemy2
\ No newline at end of file
+geoalchemy2==0.17.1setuptools<70.0.0
----------------------------------------------------------------------

PATCH FOR: 04_fire_horse.ipynb
======================================================================
--- ORIGINAL/04_fire_horse.ipynb
+++ MODIFIED/04_fire_horse.ipynb
@@ -289,6 +289,7 @@
     ")\n",
     "\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"comp_street_network.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -358,6 +359,7 @@
     ")\n",
     "\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"comp_sidewalk_any.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -427,6 +429,7 @@
     ")\n",
     "\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"comp_highway_footway.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -496,6 +499,7 @@
     "\n",
     "fig.tight_layout()\n",
     "\n",
+    "plt.savefig(\"comp_sidewalk_unique.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -643,6 +647,7 @@
     "position = fig.add_axes([1.0, 0.25, 0.02, 0.5])\n",
     "fig.colorbar(axs[2].collections[0], cax=position)\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"comp_steps.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -879,7 +884,7 @@
     "\n",
     "\n",
     "# %%\n",
-    "# Compute buffer areas in km²\n",
+    "# Compute buffer areas in km\u00b2\n",
     "areas = pd.concat(\n",
     "    [\n",
     "        pd.read_sql(\n",
@@ -1077,4 +1082,4 @@
  },
  "nbformat": 4,
  "nbformat_minor": 5
-}
+}
\ No newline at end of file
----------------------------------------------------------------------

PATCH FOR: setup.sql
======================================================================
--- ORIGINAL/setup.sql
+++ MODIFIED/setup.sql
@@ -70,11 +70,13 @@
 		SELECT belevv.elevation
 		FROM heightdata.graz_dem_vector_square belevv
 		WHERE ST_Intersects(ST_StartPoint(s.geom_32633), belevv.geom)
+		LIMIT 1
 	) AS elv_start,
 	(
 		SELECT belevv.elevation
 		FROM heightdata.graz_dem_vector_square belevv
 		WHERE ST_Intersects(ST_EndPoint(s.geom_32633), belevv.geom)
+		LIMIT 1
 	) AS elv_end
 FROM heightdata.streets_graz_dumpsegments s,
 	heightdata.graz_dem_vector_square belevv
----------------------------------------------------------------------

NEW FILE (not in original source): setup_complete.lock
======================================================================

NEW FILE (not in original source): table_10_surface.tex
======================================================================

PATCH FOR: 01_fire_horse.ipynb
======================================================================
--- ORIGINAL/01_fire_horse.ipynb
+++ MODIFIED/01_fire_horse.ipynb
@@ -385,7 +385,7 @@
        "<span style=\"float: left; margin-left: 5px; line-height: 15px; cursor: pointer; opacity: 0.7;\"\n",
        "      onmouseover=\"this.style.opacity='1'\"\n",
        "      onmouseout=\"this.style.opacity='0.7'\"\n",
-       "      title=\"Extension loaded. This cell output contains code that enables plot interactivity, it should not be removed.\">ⓘ</span>\n",
+       "      title=\"Extension loaded. This cell output contains code that enables plot interactivity, it should not be removed.\">\u24d8</span>\n",
        "</div>\n",
        "\n"
       ]
@@ -460,7 +460,8 @@
     "if hv.Store.current_backend == \"bokeh\":\n",
     "    chord.opts(label_text_font_size=\"48pt\")\n",
     "\n",
-    "chord"
+    "chord",
+    "\nhv.save(chord, \"chord_diagram.png\")\n"
    ]
   },
   {
@@ -532,6 +533,7 @@
     "ax.set_ylabel(\"type\")\n",
     "sns.despine()\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"highway1.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -590,6 +592,7 @@
     "sw_union_df.plot(ax=ax, color=\"#8fbe50\", linewidth=1.2)  # plot sidewalk data\n",
     "\n",
     "plt.axis(False)\n",
+    "plt.savefig(\"sidewalk_py.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -657,6 +660,7 @@
     ")  # plot footway sidewalk data\n",
     "\n",
     "plt.axis(False)\n",
+    "plt.savefig(\"hwfwfwsw_py.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -880,7 +884,7 @@
     "ax.annotate(\n",
     "    xy=[15.452, 47.07499],\n",
     "    va=\"bottom\",\n",
-    "    text=\"Elisabethstraße\",\n",
+    "    text=\"Elisabethstra\u00dfe\",\n",
     "    color=\"k\",\n",
     "    rotation=23,\n",
     "    fontsize=\"14\",\n",
@@ -906,6 +910,8 @@
     "\n",
     "\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"unique_sidewalk_py.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
+    "plt.savefig(\"mapping_approach_b.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -1013,6 +1019,7 @@
     "sns.despine()  # remove top and right spines\n",
     "fig.tight_layout()\n",
     "\n",
+    "plt.savefig(\"surface.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -1095,7 +1102,7 @@
     "ax.annotate(\n",
     "    xy=(0, 660),\n",
     "    va=\"top\",\n",
-    "    text=\"DEM Source:\\nDigitaler Atlas Steiermark Amt \\nder Steiermärkischen Landesregierung\",\n",
+    "    text=\"DEM Source:\\nDigitaler Atlas Steiermark Amt \\nder Steierm\u00e4rkischen Landesregierung\",\n",
     "    color=\"w\",\n",
     "    size=12,\n",
     ")  # add annotation\n",
@@ -1372,7 +1379,7 @@
     "\n",
     "query_incline = \"\"\"\n",
     "SELECT slope AS slope_dem,\n",
-    "CAST(ABS(REGEXP_REPLACE(graz_pgr.tags_h -> 'incline', '[%%°]', '', 'g')::numeric) as FLOAT) as slope_osm\n",
+    "CAST(ABS(REGEXP_REPLACE(graz_pgr.tags_h -> 'incline', '[%%\u00b0]', '', 'g')::numeric) as FLOAT) as slope_osm\n",
     "FROM graz_pgr\n",
     "WHERE graz_pgr.tags_h -> 'incline' NOT IN ('up', 'down')\n",
     "AND graz_pgr.tags_h -> 'incline' IS NOT NULL\n",
@@ -1625,6 +1632,7 @@
     "ax.set_ylabel(\"count\")\n",
     "sns.despine()  # remove top and right spines\n",
     "fig.tight_layout()  # tight layout\n",
+    "plt.savefig(\"kerb_height.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -1707,6 +1715,7 @@
     "ax.grid(\"major\", axis=\"y\", ls=\"--\", lw=0.5, c=\"grey\", alpha=0.2)\n",
     "\n",
     "fig.tight_layout()\n",
+    "plt.savefig(\"width.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -1813,4 +1822,4 @@
  },
  "nbformat": 4,
  "nbformat_minor": 5
-}
+}
\ No newline at end of file
----------------------------------------------------------------------

PATCH FOR: setup.sh
======================================================================
--- ORIGINAL/setup.sh
+++ MODIFIED/setup.sh
@@ -16,49 +16,46 @@
 INNER_FILE="$SCRIPT_DIR/DATA/graz_inner.gpkg"
 HEX_FILE="$SCRIPT_DIR/DATA/hex500.gpkg"
 
-# psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d postgres -c "DROP DATABASE IF EXISTS $DB_NAME WITH (FORCE);"
-# echo "=== 0. Creating Database ==="
-# psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d postgres -c "CREATE DATABASE $DB_NAME;" || true
-# 
-# echo "=== 1. Extensions & schemas (setup.sql section 1) ==="
-# psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -v ON_ERROR_STOP=1 <<'EOSQL'
-# CREATE EXTENSION IF NOT EXISTS postgis;
-# CREATE EXTENSION IF NOT EXISTS hstore;
-# CREATE EXTENSION IF NOT EXISTS pgrouting;
-# CREATE EXTENSION IF NOT EXISTS postgis_raster;
-# CREATE SCHEMA IF NOT EXISTS heightdata;
-# CREATE SCHEMA IF NOT EXISTS sidewalk;
-# CREATE SCHEMA IF NOT EXISTS nodes;
-# CREATE SCHEMA IF NOT EXISTS viz;
-# EOSQL
-# 
-# echo "=== 2. Importing OSM data ==="
-# osm2pgsql "$OSM_FILE" -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -k -s
-# 
-# echo "=== 3. Importing OSM routing data ==="
-# osm2pgrouting -f "$OSM_FILE" -d "$DB_NAME" -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -W "$DB_PASS" -c "$SCRIPT_DIR/mapconfig.xml" --clean
-# echo "=== 4. Importing DEM raster data ==="
-# raster2pgsql -c -C -s 32633 -f rast -F -I -M -t 100x100 "$DEM_FILE" heightdata.graz_dem | psql -U "$DB_USER" -d "$DB_NAME" -h "$DB_HOST" -p "$DB_PORT"
-# 
-# echo "=== 5. Importing Graz border ==="
-# ogr2ogr -f PostgreSQL "PG:host=$DB_HOST port=$DB_PORT user=$DB_USER password=$DB_PASS dbname=$DB_NAME" "$BORDER_FILE"
-# 
-# echo "=== 5b. Importing Graz inner boundary ==="
-# ogr2ogr -f PostgreSQL "PG:host=$DB_HOST port=$DB_PORT user=$DB_USER password=$DB_PASS dbname=$DB_NAME" "$INNER_FILE"
-# 
-# echo "=== 5c. Importing hex500 ==="
-# ogr2ogr -f PostgreSQL "PG:host=$DB_HOST port=$DB_PORT user=$DB_USER password=$DB_PASS dbname=$DB_NAME" "$HEX_FILE"
-# 
-# echo "=== 6. Creating tablespace on /home if needed ==="
-# #sudo mkdir -p /home/pg_spatial_data
-# #sudo chown postgres:postgres /home/pg_spatial_data
-# #psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -c "CREATE TABLESPACE home_space LOCATION '/home/pg_spatial_data';" || true
-# 
-# echo "=== 7. Running setup.sql ==="
-# psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -v ON_ERROR_STOP=1 -f "$SCRIPT_DIR/setup.sql"
+psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d postgres -c "DROP DATABASE IF EXISTS $DB_NAME WITH (FORCE);"
+echo "=== 0. Creating Database ==="
+psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d postgres -c "CREATE DATABASE $DB_NAME;" || true
 
-echo "=== 8. Running comparison.sql ==="
-psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -v ON_ERROR_STOP=1 -f "$SCRIPT_DIR/comparison.sql"
+echo "=== 1. Extensions & schemas (setup.sql section 1) ==="
+psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -v ON_ERROR_STOP=1 <<'EOSQL'
+CREATE EXTENSION IF NOT EXISTS postgis;
+CREATE EXTENSION IF NOT EXISTS hstore;
+CREATE EXTENSION IF NOT EXISTS pgrouting;
+CREATE EXTENSION IF NOT EXISTS postgis_raster;
+CREATE SCHEMA IF NOT EXISTS heightdata;
+CREATE SCHEMA IF NOT EXISTS sidewalk;
+CREATE SCHEMA IF NOT EXISTS nodes;
+CREATE SCHEMA IF NOT EXISTS viz;
+EOSQL
+
+echo "=== 2. Importing OSM data ==="
+osm2pgsql "$OSM_FILE" -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -k -s --middle-database-format legacy
+
+echo "=== 3. Importing OSM routing data ==="
+osm2pgrouting -f "$OSM_FILE" -d "$DB_NAME" -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -W "$DB_PASS" -c "$SCRIPT_DIR/mapconfig.xml" --clean
+echo "=== 4. Importing DEM raster data ==="
+raster2pgsql -c -C -s 32633 -f rast -F -I -M -t 100x100 "$DEM_FILE" heightdata.graz_dem | psql -U "$DB_USER" -d "$DB_NAME" -h "$DB_HOST" -p "$DB_PORT"
+
+echo "=== 5. Importing Graz border ==="
+ogr2ogr -f PostgreSQL "PG:host=$DB_HOST port=$DB_PORT user=$DB_USER password=$DB_PASS dbname=$DB_NAME" "$BORDER_FILE"
+
+echo "=== 5b. Importing Graz inner boundary ==="
+ogr2ogr -f PostgreSQL "PG:host=$DB_HOST port=$DB_PORT user=$DB_USER password=$DB_PASS dbname=$DB_NAME" "$INNER_FILE"
+
+echo "=== 5c. Importing hex500 ==="
+ogr2ogr -f PostgreSQL "PG:host=$DB_HOST port=$DB_PORT user=$DB_USER password=$DB_PASS dbname=$DB_NAME" "$HEX_FILE"
+
+echo "=== 6. Creating tablespace on /home if needed ==="
+#sudo mkdir -p /home/pg_spatial_data
+#sudo chown postgres:postgres /home/pg_spatial_data
+#psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -c "CREATE TABLESPACE home_space LOCATION '/home/pg_spatial_data';" || true
+
+echo "=== 7. Running setup.sql ==="
+psql -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -d "$DB_NAME" -v ON_ERROR_STOP=1 -f "$SCRIPT_DIR/setup.sql"
 
 echo "=== Done ==="
 unset PGPASSWORD
----------------------------------------------------------------------

PATCH FOR: 02_fire_horse.ipynb
======================================================================
--- ORIGINAL/02_fire_horse.ipynb
+++ MODIFIED/02_fire_horse.ipynb
@@ -302,6 +302,8 @@
     "\n",
     "fig.tight_layout(pad=0)\n",
     "\n",
+    "plt.savefig(\"experiment1_TUGRAZ.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
+    "plt.savefig(\"experiment2_Park.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -343,12 +345,14 @@
     "\n",
     "\n",
     "def profiles(ax, df_plot, mythema, color_dict=None):\n",
+    "    if df_plot.empty: print(\"Warning: df_plot is empty, skipping plot.\"); return\n",
     "    df_plot = df_plot.copy()\n",
     "    thema = mythema\n",
     "\n",
     "    from scipy.signal import savgol_filter\n",
     "\n",
-    "    df_plot[\"elevation\"] = savgol_filter(df_plot[\"elevation\"], 5, 2)\n",
+    "    if len(df_plot[\"elevation\"]) >= 5:\n",
+    "        df_plot[\"elevation\"] = savgol_filter(df_plot[\"elevation\"], 5, 2)\n",
     "    df_plot[\"change\"] = df_plot[thema].ne(df_plot[thema].shift().bfill()).astype(int)\n",
     "    df_plot[\"subgroup\"] = df_plot[\"change\"].cumsum()\n",
     "    df_plot.index += df_plot[\"subgroup\"].values\n",
@@ -464,6 +468,7 @@
     "\n",
     "\n",
     "def profiles(ax, df_plot, mythema, color_dict=None):  # plot elevation profile\n",
+    "    if df_plot.empty: print(\"Warning: df_plot is empty, skipping plot.\"); return\n",
     "    df_plot = df_plot.copy()  # copy dataframe to avoid warning\n",
     "    thema = mythema  # set thema\n",
     "\n",
@@ -555,6 +560,7 @@
     "\n",
     "fig.tight_layout()\n",
     "\n",
+    "plt.savefig(\"experiment1_TUGraz_elvprofile_ped.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -698,6 +704,7 @@
     "\n",
     "fig.tight_layout()\n",
     "\n",
+    "plt.savefig(\"experiment2_Park.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -742,12 +749,14 @@
     "\n",
     "\n",
     "def profiles(ax, df_plot, mythema, color_dict=None):\n",
+    "    if df_plot.empty: print(\"Warning: df_plot is empty, skipping plot.\"); return\n",
     "    df_plot = df_plot.copy()\n",
     "    thema = mythema\n",
     "\n",
     "    from scipy.signal import savgol_filter\n",
     "\n",
-    "    df_plot[\"elevation\"] = savgol_filter(df_plot[\"elevation\"], 5, 2)\n",
+    "    if len(df_plot[\"elevation\"]) >= 5:\n",
+    "        df_plot[\"elevation\"] = savgol_filter(df_plot[\"elevation\"], 5, 2)\n",
     "    df_plot[\"change\"] = df_plot[thema].ne(df_plot[thema].shift().bfill()).astype(int)\n",
     "    df_plot[\"subgroup\"] = df_plot[\"change\"].cumsum()\n",
     "    df_plot.index += df_plot[\"subgroup\"].values\n",
@@ -821,6 +830,7 @@
     "fig.supylabel(\"Elevation [m]\", x=0.055)\n",
     "\n",
     "\n",
+    "plt.savefig(\"experiment2_Park_elvprofile_wheel.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   },
@@ -865,12 +875,14 @@
     "\n",
     "\n",
     "def profiles(ax, df_plot, mythema, color_dict=None):\n",
+    "    if df_plot.empty: print(\"Warning: df_plot is empty, skipping plot.\"); return\n",
     "    df_plot = df_plot.copy()\n",
     "    thema = mythema\n",
     "\n",
     "    from scipy.signal import savgol_filter\n",
     "\n",
-    "    df_plot[\"elevation\"] = savgol_filter(df_plot[\"elevation\"], 5, 2)\n",
+    "    if len(df_plot[\"elevation\"]) >= 5:\n",
+    "        df_plot[\"elevation\"] = savgol_filter(df_plot[\"elevation\"], 5, 2)\n",
     "    df_plot[\"change\"] = df_plot[thema].ne(df_plot[thema].shift().bfill()).astype(int)\n",
     "    df_plot[\"subgroup\"] = df_plot[\"change\"].cumsum()\n",
     "    df_plot.index += df_plot[\"subgroup\"].values\n",
@@ -948,6 +960,7 @@
     "fig.supylabel(\"Elevation [m]\", x=0.02)\n",
     "fig.tight_layout()\n",
     "\n",
+    "plt.savefig(\"experiment2_Park_elvprofile_ped.png\", dpi=dpi_plot, bbox_inches=\"tight\")\n",
     "plt.show()"
    ]
   }
@@ -973,4 +986,4 @@
  },
  "nbformat": 4,
  "nbformat_minor": 5
-}
+}
\ No newline at end of file
----------------------------------------------------------------------

NEW FILE (not in original source): table_11_highway.tex
======================================================================

PATCH FOR: import_comparison.sh
======================================================================
--- ORIGINAL/import_comparison.sh
+++ MODIFIED/import_comparison.sh
@@ -33,9 +33,9 @@
 EOSQL
 
 echo "=== 2. osm2pgsql — full OSM data ==="
-osm2pgsql "$GRAZ_OSM"     -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -j -s -p graz
-osm2pgsql "$SALZBURG_OSM" -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -j -s -p salzburg
-osm2pgsql "$LINZ_OSM"     -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -j -s -p linz
+osm2pgsql "$GRAZ_OSM" -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -k -s -p graz --middle-database-format legacy
+osm2pgsql "$SALZBURG_OSM" -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -k -s -p salzburg --middle-database-format legacy
+osm2pgsql "$LINZ_OSM" -r xml -c -d "$DB_NAME" -U "$DB_USER" -H "$DB_HOST" -P "$DB_PORT" -k -s -p linz --middle-database-format legacy
 
 echo "=== 3. osm2pgrouting — routing network ==="
 osm2pgrouting -f "$GRAZ_OSM"     -d "$DB_NAME" -U "$DB_USER" -h "$DB_HOST" -p "$DB_PORT" -W "$DB_PASS" --clean --prefix graz_pgr_     --tags --addnodes --attributes
----------------------------------------------------------------------

NEW FILE (not in original source): table_12_kerbs.tex
======================================================================

NEW FILE (not in original source): table_13_city_comparison.tex
======================================================================
